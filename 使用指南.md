# Hexo Butterfly 博客使用指南

## 快速命令

```bash
# 进入博客目录
cd my-blog

# 安装依赖（首次或更新后）
npm install

# 本地预览（实时更新）
npm run server

# 生成静态文件
npm run build

# 部署上线
npm run deploy
```

---

## 添加新文章

### 方式一：命令行创建

```bash
hexo new "文章标题"
```

这会在 `source/_posts/` 目录下创建一个新的 Markdown 文件。

### 方式二：手动创建

在 `source/_posts/` 文件夹下新建 `.md` 文件。

---

## 文章格式模板

```markdown
---
title: 你的文章标题
date: 2026-03-28 12:00:00
tags:
  - 标签1
  - 标签2
categories: 分类名称
---

正文内容...
```

### 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| title | 是 | 文章标题 |
| date | 是 | 创建日期时间 |
| tags | 否 | 标签（数组，可多个） |
| categories | 否 | 分类（一个） |
| comments | 否 | 是否开启评论（默认 true） |
| toc | 否 | 是否显示目录（默认 true） |

---

## 文章示例

```markdown
---
title: 我的第一篇博客
date: 2026-03-28 10:30:00
tags:
  - 教程
  - 入门
categories: 技术
---

## 引言

这是文章的正文内容。

### 二级标题

段落内容...

- 列表项1
- 列表项2

```javascript
// 代码块
const hello = 'world';
```

> 引用内容
```

---

## 常用技巧

### 添加图片

1. 将图片放入 `source/images/` 目录
2. 在文章中使用相对路径：`![描述](/images/图片名.png)`

### 添加链接

```markdown
[显示文字](https://example.com)
```

### 嵌入音乐

```html
<iframe src="https://music.163.com/player/?id=xxx" frameborder="no"></iframe>
```

### 折叠内容

```markdown
<details>
<summary>点击展开</summary>
隐藏内容
</details>
```

---

## 本地预览

运行 `npm run server` 后，访问 http://localhost:4000 查看效果。

---

## 部署上线

```bash
npm run deploy
```

部署前确保 `_config.yml` 中的 `deploy` 配置正确。

---

## 目录结构说明

```
my-blog/
├── source/_posts/    # 博客文章（最重要）
├── source/images/    # 图片资源
├── source/tags/      # 标签页
├── source/categories/# 分类页
├── themes/butterfly/ # 主题配置
├── _config.yml      # Hexo 配置
└── package.json     # 项目配置
```
